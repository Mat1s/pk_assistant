:schedule:
  service_worker:
    every: 1m #cron: '0 * * * * *'   # Runs once per minute
    class: ServiceWorker
    # config/sidekiq.yml

# :schedule:
#   hello_world:
#     every: 1m   # Runs once per minute
#     class: HelloWorld
# Run sidekiq:

# sidekiq -r ./hello-scheduler.rb
